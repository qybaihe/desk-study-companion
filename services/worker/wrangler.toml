name = "banxue-api"
main = "src/index.ts"
compatibility_date = "2025-01-15"

# 自定义域名。默认的 *.workers.dev 在国内被 DNS 污染，必须走自有域名。
[[routes]]
pattern = "sheepy.timoz.me"
custom_domain = true

# 机密用命令行设置，不写进这个文件：
#   wrangler secret put DATABASE_URL
#   wrangler secret put API_TOKEN
