import Foundation

/// 复制成同目录下的 `Secrets.swift` 再填值。Secrets.swift 已被 gitignore。
///
/// token 就是部署 Worker 时 `wrangler secret put API_TOKEN` 设的那个值。
enum Secrets {
    static let apiToken = "在这里填 Worker 的 API_TOKEN"
}
